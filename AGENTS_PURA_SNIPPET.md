# AGENTS.mdへの追記用 — PURA v1.0

このファイル名のまま自動読込されることは前提にしない。開始時に既存のAGENTS.mdを読み、その指示・権限を保持しながら、以下の節を追加・統合する。同等の節があれば重複させない。AGENTS.mdがなければ、適用される親階層の指示を確認してからプロジェクト用に作る。

---

## PURA project guidance

PURAは、透明な色付き水滴をつかみ、滑らせ、道中の同色を連続吸収する、ゲームとデジタルトイの中間の体験。美しさは飾りではなく報酬。入力・連鎖を止めず、動いている間の質感を磨く。

### 読むもの

- 最新の仕様正本: `docs/pura-v1/REQUIREMENTS.md`
- 作業前に `docs/pura-v1/STATUS.md` と `DECISIONS.md` を確認。
- 初回は正本全体と `PURA_NORTH_STAR.md`、必要な `VALIDATION.md` を読む。
- 美術作業では `references/visual-target.png` を確認。表示できなければ未確認と記録。
- 旧 `docs/pura-next/` は参考履歴。新しいユーザー指示と現地の実装進捗を優先し、古い状態へ戻さない。

### 守ること

- 既存の独自WebGL版と作業中の変更を保護。現行にThree.jsが導入済みとは仮定しない。
- 質量・純度・主成分色・異色反発・条件付き混色／分離を継承。S/M/Lや異色即破裂の古いルールを混ぜない。
- 描画更新で物理の質量や当たり判定を変えない。融合演出待ちで入力や次の吸収を止めない。
- 半径二乗の質量則を、3D化のついでに球体積則へ変えない。
- 新しい自動加速・磁力コンボ・自動補充を足さない。元の弱い引力は別物。
- 初期案はWebGL2/Three.js。別方式・追加依存は理由を残し、全面移植を先行させない。
- 全面レイトレーシング、実行時AI API、ログイン・課金は初期要件ではない。

### 作業と検証

remote、HEAD、未コミット差分、Node/パッケージ管理、適用AGENTSを最初に確認。無断のreset/clean/stash、無関係な変更の巻き込みをしない。新しいfeatureブランチかworktreeを使うかは現在地と既存指示を見て決める。

コマンドは実際のpackage.jsonとロックファイルを確認して選ぶ。基準版のbuildは型検査を含まない。ビルド・型検査・対象テストを区別する。存在しないテストコマンドを成功扱いにしない。

ビルド成功、実ブラウザ確認、実機性能、RYOの感覚評価を分ける。できない検証は「未検証」と明記。目標を実測と混同しない。

初回はM0→M1。M2以降は実画面へのフィードバック後。段階内の安全な細部は進め、コア変更・費用・公開・破壊的変更は指示なしで行わない。

終了時はSTATUSへ、変更・起動方法・実行コマンドと結果・証拠・未達・次の一手を記録する。日本語で報告する。GitHub push、公開、デプロイは別途明示指示を得る。
