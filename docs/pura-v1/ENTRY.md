# 入口の整理と、通常URLの切り替え（2026-09-26）

ユーザーが「おすすめ順に実装」（1: 入口の整理 → 3: 正式な入口の切り替え）を承認。

## 1. 入口の整理

- 全プレイ画面（はじめる・ステージ・スコア・自由・三つの道）の上部に共通のモード切り替え `ModeNav`（`src/experiments/mode-nav.tsx`）。要件7.1に従い、メニュー画面を挟まず各画面ですぐ遊べる。案内であって関門ではない。
- ステージ画面では「ステージ⇄スコア」をその場で切り替え、遊んでいる面を保つ（従来の画面内ボタンは廃止し、スコア時はその面のベストを表示）。
- 実験（一粒・融合）はメニューに入れず、入口画面の下部「実験室」から。元祖は「元祖PURA」から。ステージ選択の元祖サンドボックスの表示を「自由」→「サンド」に変更（自由モードとの混同を避ける）。
- PURAのロゴは全画面で通常URL（はじめる）へ戻る。

## 3. 通常URLの切り替え

- `https://blitast-studio.github.io/pura/`（クエリなし）は、元祖PURAから新しい入口プレイへ。元祖は `?play=classic` で従来どおり遊べる。未知のクエリも入口プレイへ。
- 起動の振り分けを `src/route-key.ts`（純粋関数・テストあり）と `main.tsx` の対応表に整理。各画面は必要になってから読み込む。
- 対象はGitHub Pagesの試遊配信。itch.ioの公開版（元祖）は変更していない。配信用ZIP（`artifacts/pura-html.zip`）も同じ入口になるが、アップロードはしていない。
- 戻し方: タグ `before-root-switch-20260926`（3383cec、当時の配信 `02baf45`）。`gh-pages` の今回の配信コミットをrevertするか、そのタグをビルドして配信する。

## 検証

- 型検査、110テスト成功（振り分け1件追加）。
- GPU描画のヘッドレスChrome（390×844）: 通常URLで入口プレイと「はじめる」選択中、各モードへの移動、ステージ5面→スコアで5面を保持、`?play=classic` で元祖がモード切り替えなしで表示。警告エラー0。`artifacts/entry/root-mobile.png`。
