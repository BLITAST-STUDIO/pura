# カーリング — 雫で、カーリングのルールそのまま（2026-09-27）

おすすめ順の2番目（ユーザー承認「おすすめ順開発でお願いします」）。元企画の「弾いた雫がカーリングのように滑る」を、ユーザーの方針「既存スポーツのルールを下敷きにする」（DECISIONS 2026-09-27）で遊びにした。作業ブランチ `feat/curling`（`feat/michi` から）。入口 `?play=curling`（ふたりで `&vs=two`）。

## ルール（カーリングから借りたもの）

- シートの奥にハウス（同心円、中心がボタン）、手前にホグライン。下の丸（ハック）から、ひとふでと同じ「引いて、離す」で投げる。
- 1エンドに各色4つを交互に投げる。ハンマー（最後の一投）を持たない側が先攻。
- ホグラインを越えずに止まった一投は外す。側面・奥の壁に触れた雫は場外。
- 全部止まったら数える: ハウスに触れている雫のうち、ボタンにいちばん近い色が、相手のいちばん近い雫より内側にある数だけ得点（距離はボタンから雫の縁まで）。ハウスに誰もいなければブランク（0点）。
- 点を取った側はハンマーを相手に渡す。ブランクならそのまま。
- 4エンドの合計で勝負。同点なら延長1エンド、それでも同点なら引き分け。

## PURAに合わせて変えたところ（理由）

- **同じ色は触れるとひとつになる**（PURAの核）。二つが一つの大きく重い雫になり、ボタンに届きやすく動かしにくい反面、1つとしか数えない。「くっつけて守るか、離して点を稼ぐか」の選択になる。
- **曲がらない**（雫に回転はない）。ガードの裏へ回り込む打ち方はなく、角度と当て方で勝負する。
- シートは他のモードと同じ3:4の盤面に短く収めた。滑りには一定の摩擦（60）を加え、滑ってから約3秒以内に止まるようにした（元祖の減衰だけだと止まるまで8秒ほどかかり、交互に投げる間が間延びするため）。強さの目安: 0.6でボタン、ハウスは約0.46〜0.71、0.8以上ははじき出し。
- 相手: 「ひとりで」はCPU、「ふたりで」は1台を交代で。CPUは実際の物理で約280通り（角度×強さと、相手の雫を狙う強打）を先読みし、「今エンドが終わったら何点か」とボタンへの近さで選び、人のように少しぶれる（角度±1.2°、強さ±0.025程度）。1回の先読みは約30ミリ秒で、盤面を止めずに数フレームに分けて行い、打つ前に点線を0.7秒見せる。

## 画面

スコアボード（エンドごとの点、合計、ハンマー●）、残りの雫、今の形での得点（「いま ローズ 1点」）、外れたときの一言（「ホグラインに届かず、外れた」「場外」）、エンドの結果と「次のエンドへ」、試合結果。CPU戦の通算成績を記録。モード切り替えに「カーリング」を追加（はじめる・道・ひとふで・カーリング・スコア・自由）。床にハウスの円・中心線・ホグライン・ハックを描く（描画側の追加。他の画面は公開版と画素差0）。

## 検証

- 型検査・147テスト・ビルド成功。`tests/curling.test.ts`: 得点の数え方（最も近い側、相手の最も近い雫より内側、縁が触れていれば有効、ブランク）、強さ0.6で4フィートの円の中・0.35はホグ・1.0は場外、先攻と交互、ハンマーの受け渡し（ブランクは据え置き）、延長、同色の融合は1つと数える、強打で相手をはじき出す、人が操作する側だけ触れる、CPUが空のシートで得点する一投と、ボタンの相手に対して形を良くする一投を選ぶ。
- CPUどうしで4エンドを最後まで打ち切り（2–3）、ハンマーの受け渡しを確認。
- ブラウザ（ヘッドレスChrome、GPU）: CPU戦で1エンドを通し（CPUが考えて点線を見せて投げる、こちらの4投、エンドの得点とスコアボード）、警告エラー0。`artifacts/curling/end-scored.png`。

## 未確認・次の一手

- CPUの強さ（今は手堅く強め）、1エンドの雫の数、エンド数、強さの幅の手触りは、ユーザーの試遊で調整。
- 回転（曲がり）を入れるか、盤面を縦に長くするかは、遊んだ感触しだい。
