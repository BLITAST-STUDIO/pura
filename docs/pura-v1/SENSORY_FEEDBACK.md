# 壁の手応えと、音・振動

2026-09-24、Claude Code（Opus 5.5）へ引き継ぎ。ユーザーが現状の感触と見た目を高評価したうえで、さらなる昇華の提案（2→1→3/4の順）を承認。本書は2と1の実装記録。作業ブランチ `feat/sensory-feedback`、復元タグ `chapters-approved-20260924`（2443937）。

試遊: https://blitast-studio.github.io/pura/?play=chapters （一粒・融合の実験にも同じ音）

## 2. ゲーム面の壁の手応え

- 以前は融合／3面の描画で壁の衝撃が常に0に固定され、M1で承認された「壁で潰れて戻る」反応が起きていなかった。
- `PuraSim.onContact` を追加。壁・固定石・滴同士の跳ね返りで実際に加わった速度変化を観測者へ渡すだけで、物理の結果は観測の有無でビット単位に一致（テストで確認）。リサイズや分離時の位置補正は接触として報告しない。
- `FusionSimulation.contacts` が1フレーム分を保持し（上限512件）、描画側が滴ごとに合算して共有の `DropletMotion` へ渡す。M1の `DropletSimulation.wallImpulse` と同じ量になることを1%以内で確認（差はM1側が同じ段の0.8%減衰も数えるため）。
- 固定石にも同じ反応が出る。石の形に沿って潰れる変形は未実装（提案3の範囲）。

## 1. 音と振動

音声ファイルは使わず、Web Audioで毎回合成する。容量増加なし。

| 出来事 | 音 | 振動 |
| --- | --- | --- |
| つかむ | 小さな上向きの泡音「ぷ」＋こもった接触音 | 8 ms |
| 壁・石に当たる | 低い「とっ」。石は少し硬い高域を足す | 衝撃に応じ5〜18 ms |
| 融合 | 音程が上がる水滴の「とぷん」。純色は澄んだ余韻、混色は濁ったうなり | 純色1回／混色2回 |
| 純度を保って全部集めた | ガラス鈴1音 | 10 ms |
| 輪に届いた | 2音。全目標達成は4音の分散和音 | 2〜3回 |
| 一手戻す | 短い巻き戻し音 | 6 ms |

- 音程は滴の大きさ（大きいほど低い）、左右の定位は盤面上の位置に従う。色ごとに鈴の高さが違う（シアン784 Hz、ローズ659 Hz）。
- 壁へ押し付け続ける間は毎段で接触が起きるため、0.12秒以上静かだった後の最初の接触だけを「衝突」として鳴らす。弱い接触（速度変化70未満）は鳴らさない。
- 軽い部屋の残響と、同時多発時の音割れを防ぐリミッターを通す。
- 振動: Androidなどタッチ端末のVibration API。iPhoneのSafariには同APIがないため、iOS 18以降で知られる非表示スイッチ部品のクリックによるシステム触覚を試す（強さ固定・ユーザー操作の中でのみ働く可能性）。**実機未検証**。PCでは振動設定を表示しない。
- 設定: 3面は操作列のスピーカーボタンと「遊び方と表示」、実験画面は設定パネルに「音」「振動」。ブラウザごとに `pura-flow-sensory-v1` へ保存。`?sound=off` / `?haptics=off` はその訪問だけ消音。
- iPhoneはマナーモード（消音スイッチ）中はWeb Audioが鳴らない。最初のタッチでは音声の起動が間に合わず、1回目のつかむ音が鳴らない場合がある。

## 検証

- 型検査成功、71テスト成功（新規16: 接触7、音と振動の割当て9）。配信用ZIP 338.5 KB。500 kB超のチャンク警告は従来どおり（共有チャンク名が `useSensoryFeedback-*.js` に変わったのはビルドツールの命名で、内容はthree.jsと共有コード）。
- 音量: 最終版をOfflineAudioContextで書き出し、ピークを計測（`artifacts/sensory/audio-levels.json`）。つかむ −21.9、強い壁 −18.3、融合 −15.7、全達成 −11.6 dBFS。最悪の同時多発でも −1.9 dBFSで音割れなし。聴感・スマホスピーカーでの聞こえ方は未評価。
- ヘッドレスChrome（Mac、SwiftShader）で公開URLを実操作: 第1面で壁つぶれ0.087、つかむ→壁→融合2回→純度達成→輪へ届く→一手戻すの各音が1回ずつ、戻した後に和音が再生されないこと、消音ボタンの保存。第2面390×844で石への衝突音とつぶれ0.046。一粒の実験で壁つぶれ0.072、融合の実験で融合音と壁つぶれ0.110。コンソールの警告・エラー0。証拠 `artifacts/sensory/`。
- ヘッドレスのため、実際に音を聞いた確認・実機の振動・iPhoneの触覚・タッチ操作は未検証。音の好み・大きさ・うるささの判断はユーザーの試遊待ち。

## 調整（2026-09-24）

ユーザーの実機試遊で音は好評。「わかるかわからないかの瀬戸際くらいで、エッジを丸める（高音をほんのわずかにカット）」との要望により、全体の出口に4.5 kHz以上を−2.5 dBする緩いシェルフと、11 kHz付近の緩いローパス（Q 0.5）を追加。音量・各音の設計は変更なし。聴感での確認はユーザー待ち。

ユーザーが調整後の音を「完璧」と承認。保存点 `sensory-approved-20260924`。
