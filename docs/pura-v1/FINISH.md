# 仕上げ（2026-09-27）

ユーザー承認（「仕上げをまずやりましょう」）。見た目の方向を決めた時点で後回しにしていた「形が固まってからの仕上げ」（DESIGN_DIRECTION.md）。遊びの中身・物理・盤面の描画は変えていない（主要5画面で公開版と画素差0）。作業ブランチ `feat/finish`（`main` から）。

## 入れたもの

- **クリアの余韻**（`clear-glow.tsx`、`stages.css`）: クリアすると盤面に光の輪が広がり、柔らかな明るみが消えていく。結果の表示はその約0.8秒後にふわっと出る（すぐに「次へ」を迫らず、できた雫を眺める間）。道・スコア（元祖8ステージ）・ひとふで（1打の「ひとふで」演出以外）・カーリング（勝敗が決まったとき）。揺れを控えめにする設定では出さない。
- **盤面の入れ替わり**（描画側）: 面・ホールを変えたとき、画面を開いたときは盤面が0.45秒でフェードイン、「やり直す」は0.26秒。切り替えが「切れる」のではなく「入れ替わる」。
- **初回の一文**（`intro-line.tsx`）: 初めて開いたときだけ、はじめるの盤面に「ひとつに、澄んでいく。」が浮かんで約6.5秒で消える。タップ不要・操作を妨げない（要件7.1）。`?intro=1` で再表示。
- **読み込み画面**（`index.html`）: 旧デザインの「LIQUID CORE / PURA」を、アプリと同じ雫の輪郭のマーク＋「PURA.」と細い光の線に。明るい画面・暗い画面の両方。
- **言葉と見出しの統一**:
  - 見出しは「モード名｜位置」（はじめる｜FLOW、道｜1-1、ひとふで｜HOLE 04、カーリング｜END 1、スコア｜STAGE 03、自由｜FREE）。盤面上の英字表示は道を「PATH」に。
  - ブラウザのタブ名をモードごとに（「PURA — 道」など、はじめるは「PURA」）。
  - 下部の一文から「試作」「テストプレイ」を外し、モード切り替えと重なるリンクを整理（はじめるの実験室・元祖PURA、道の元祖8ステージ・三つの道（旧）は残す）。
  - スコア: テスト期の比較設定（大きさ・混ぜ方）とサンドボックスを隠し、決定済みの既定（大きめ・押し込み0.35秒）で固定。
- 画像 `artifacts/finish/boot-intro-clear.png`。

## 検証

- 型検査・149テスト・ビルド成功（初回の一文が一度だけ出る判定のテストを追加）。
- ブラウザ（ヘッドレスChrome、GPU）: 読み込み画面、初回の一文（2回目の訪問では出ない）、道1-1をクリアして「光の輪→約0.8秒後に結果」の順（結果の不透明度 0→0.31→0.65→1）、タブ名と見出し、スコア画面が8ステージのみで比較設定なし、警告エラー0。
- 主要5画面（はじめる・道2-2・ひとふで5番・カーリング・スコア3）の盤面は公開版と画素差0。
