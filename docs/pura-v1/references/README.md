# 参照資料

`PURAの強み.txt` は共有された原文を改変せずに保存したもの。文書にある例示の全てを実装する指示ではない。

`visual-target.png` はこの会話で生成したコンセプト画像の元ファイル。質感・透明感・色・空気感の参考にする。実際のゲーム画面や実行性能の証拠ではなく、画像内の文字・低い視点・ボケ・飛沫等を全て採用する指定でもない。

このディレクトリは設計参照用。ゲームの起動時必須素材としてそのままバンドルしない。
