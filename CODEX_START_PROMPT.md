# PURA FLOW — Codex開始指示

要件定義v1.0.1 / 2026-09-16。既存PURAのローカル作業フォルダを開き、このパッケージのファイルを追加してから使う。

- 正式名称（今回の昇華版プロジェクト名）: **PURA FLOW**
- 推奨フォルダ名 / 作業ディレクトリ名: **`pura-flow`**
- 作品・ブランド名: **PURA**

## 開始指示

```text
あなたはPURA FLOWの実装担当です。公開上の作品名はPURA、今回の昇華版プロジェクト名はPURA FLOWです。
対象は既存リポジトリ BLITAST-STUDIO/pura です。

透明でカラフルな、水滴に近い物質を指やマウスでつかみ、滑らせ、
同色の雫を連続で取り込む快感UXを主役にした、おしゃれな体験を作ります。
ゲームとデジタルトイの中間を目指します。
ルールを増やすのではなく、動いている最中の美しさと触り心地を磨いてください。

【読み込み】
適用される既存のAGENTS.mdと現在の作業状態を確認してください。
AGENTS_PURA_SNIPPET.mdを既存の指示を壊さず統合してください。
次に以下を読んでください。
- docs/pura-v1/REQUIREMENTS.md（最新版の正本）
- docs/pura-v1/PURA_NORTH_STAR.md
- docs/pura-v1/STATUS.md
- docs/pura-v1/DECISIONS.md
- docs/pura-v1/VALIDATION.md
- docs/pura-v1/SOURCE_AUDIT.md
画像を表示できる場合は references/visual-target.png も確認してください。
この画像は物質感の目標であり、完成画像を背景に貼る指示ではありません。

旧 docs/pura-next/ より新要件を優先しますが、既にローカルで進んだ実装・承認・
検証記録がある場合は読み取り、古い未着手状態へ戻さないでください。
資料上の基準HEADは067d45b9d7eb42f0bbccdb43405aa70f41d952b1ですが、
実際のHEADと差分を確認し、違いを記録してください。

【今回の作業範囲】
M0の現状確認から、M1の「触れる水滴1個」の実装までです。
重大な障害がなければ、計画だけの回答で終わらず、起動できる試作まで進めてください。
融合実装・即プレイの通常入口への切替・全ステージ移植・公開は今回は行いません。

1. remote、HEAD、ブランチ、未コミット変更、適用指示、実行環境を確認する。
   他の変更を壊さず、適切なfeatureブランチまたはworktreeで旧版を保護する。
   現在の変更を無断で戻す・削除する・まとめてコミットすることはしない。
2. package.jsonと依存管理方式を読み、旧版の起動、ビルド、型検査、既存テストを確認する。
   存在しないnpm ci/npm testの実行を前提にしない。既存問題と新規問題を分ける。
3. sim、renderer、palette、levels、入力、保存の境界を確認する。
   現行版は独自WebGL。Three.jsは必要に応じて新規導入する。
   既存の物理と滑走を比較基準として残す。
4. 短い実行計画をSTATUSに記載し、初期案のThree.js/WebGL2で独立実験画面を作る。
   候補入口は ?lab=droplets。既存の通常入口は置き換えない。
   技術的に別案が必要なら、具体的な理由を記録する。
5. 暗く落ち着いた盤面、俯瞰寄りの固定カメラ、一滴を実装する。
   三色の材質切替、ドラッグ、指離し、滑走、壁での変形を確認可能にする。
   表面反射、厚みのある色、盤面の屈折、接地感、短い変形と収束を作る。
   通常盤面と屈折が分かる検査盤面を切り替え可能にする。
6. 描画と物理の状態を分離し、調整値を少数の設定へまとめる。
   M2の融合イベントと形状差し替えを見据えるが、今は融合機能まで作り込まない。
   ゲーム内質量則を3D体積則へ置き換えない。投射速度を二重に加算しない。
7. 一滴でも実際に操作できる状態を作り、ビルド・型検査・実行可能なテストを実施する。
   利用できるブラウザで描画と入力を確認し、可能な計測を行う。
   画面・GPU・実機を確認できない場合は未検証と明記する。
8. STATUSを更新し、変更、起動方法、旧版と試作の開き方、実行コマンドと結果、
   実測条件、画像や動画の有無、未達・未検証、私が触って判断する項目を報告する。

硬い透明球を表示できただけ、スクリーンショットが綺麗なだけ、ビルドが通っただけで
M1の品質合格とはしないでください。実装完了と、私の感覚評価待ちを区別してください。

ライブラリ全更新、WebGPU専用化、別エンジン移植、実行時AI API、オンライン機能、
課金、新ルールの追加は不要です。GitHubへのpush、公開、デプロイは行わないでください。
```

## M1の実画面確認後に進むとき

```text
M1を実際に触りました。以下のフィードバックを反映し、M2へ進めてください。
［ここに、水滴の見た目・掴む反応・滑り方についての実際の感想を記入］

最新のSTATUSと要件を確認し、まず同色2個が連続した表面として融合する実装を行い、
その後5個程度で連続吸収を検証してください。融合中の入力と追加吸収を止めず、
質量・色成分・速度継承とIDの連続性を守ってください。
異色は混色が成立していない限り見た目だけつながらないようにしてください。
同じ場面・条件で旧版との比較と測定を行い、未達と実測結果を記録してください。
通常入口の切替、8ステージ統合、公開はまだ行わないでください。
```

## 次のセッションで再開するとき

```text
適用AGENTS.md、docs/pura-v1/STATUS.md、DECISIONS.mdを読み、
実際の作業ツリーと前回結果を確認してください。
最新のユーザー承認範囲内で、STATUSの次の一手を進めてください。
調査結果、実装、実行したテスト、未検証、品質判断待ちを区別し、終了時にSTATUSを更新してください。
```
